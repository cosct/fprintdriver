# Windows 引擎系数表提取与复刻笔记（EgisTouchFPEngine0575.dll v3.7.1.1）

> **状态：完成。** 系数表全部提取，Python 复刻达到离线完美分离，C 移植
> 已随驱动 [cosct/libfprint-egis0575](https://github.com/cosct/libfprint-egis0575)（libfprint/ 子树）
> v0.2.1 发布（`egis0575-matcher.c`）。本文是完整实验记录，供复核与
> 后续增强。
> [English version](windows-engine-tables.en.md)

## 1. 来源与方法

- DLL 来源：`acerdrv/Fingerprint_EGISTEC_3.7.1.1_W10x64/x64/`（Acer 官网，
  SFX14-41G 型号驱动包）。引擎 DLL 324KB，与 Animeshz 反编译版本不同
  → 反编译地址不可复用，需按签名扫描重新定位
- **方法论突破**：capstone `disasm()` 遇坏字节停摆，首轮漏检 90% 代码
  （5127 → 48826 条指令）；改为**失步重同步线性扫描**后引用链闭合
- 复刻载体：`scripts/egis_matcher.py`（Python 参考实现）；
  `tools/egis0575-matcher-test.c`（C 交叉验证 harness）

## 2. 已确认系数表（2026-09-13 提取会话）

| 表 | RVA | 验证方式 |
|---|---|---|
| sin 表 360×int32 Q16 | 0x3eac0 | 全 360 项逐点匹配 sin(i°)×65536（容差 8） |
| cos 表 360×int32 Q16 | 0x3f060 | 同上；与 sin 相距恰 1440 字节 |
| ~~脊线 FIR 抽头（13 抽头对称带通）~~ | ~~0x400f0~~ | （已被下行 0x3ffd0 定案取代：该读数实为取向9/10 核的跨界误读，峰 6572） |
| **描述子权重核（金字塔）** | 0x40130 起 | 9 抽头×2 + 11 抽头×4 对称核（峰值 118/207/308/392/425 递进），121 项与 11×11 描述子吻合 |
| **11 取向脊线模板核**（长度 5,5,5,7,7,7,9,9,9,11,11 紧凑 i32、归一化和 32768） | RVA 0x3ffd0（= 文件偏移 0x3ebd0） | 驱动 `egis0575-matcher.c` 已按 DLL 提取（fir_taps/fir_len）；取向4 为 7 抽头（早前提取误补成 9，2026-09-13 更正） |
| **atan2 LUT（u16 单调）** | 0x40320 | 严格单调 0→358（128 项），代码做 `720−x` 变换 + 舍入除法索引 |
| 传感器配置表 | 0x47794 | 值含 88/52/103 = 归一化高/传感器高/宽 |

## 3. 关键代码位点（重同步反汇编定位）

- **0x15515**：`lea rsi,[rip+…]` = 0x40130 → 卷积循环 `imul` 图像梯度×权重核
  = **描述子构建函数**
- **0x153bf/0x153e4**：`movzx word [0x40320 + idx*2]` + `720−x`
  = **梯度方向计算**

## 4. 复刻迭代记录（关键领悟：轴向 ≠ 取向）

**滤波器组重解读**：11 个核不是 11 个尺度，而是 **11 个取向的脊线匹配
滤波器**（对角取向等效支撑更长 → 抽头更多）。正确实现：对每取向 θ 做
定向 FIR → 响应取最大 = 增强图 + 取向图（可导向滤波器组）。轴向可分离
平滑实验已证伪（兴趣点塌缩、真假倒挂）。

| 变体 | 真手指票 | 冒充票 | 结论 |
|---|---|---|---|
| 32bit + Lowe + 中值 | 0 | — | 描述子太弱 |
| 128bit + Lowe + 中值 | 0-1 | — | Lowe 杀真对（邻点太像）|
| 128bit 无Lowe + 平移桶聚类 | 5,3,3 | 0,3,5 | 首现信号 |
| 256bit(16bin) | 3,2,2 | 3,2,3 | 细分不稳（SNR 限制）|
| + 轴向FIR平滑 | 0 | 3-4 | 证伪（轴向≠取向）|
| **+ 11取向滤波器组** | **9,7,8 / 5 / 7,7,6** | 3-7 | 票数仍重叠 |
| + 加权分数 Σ(128−h) | 均值156 (min107) | 均值86 (max137) | **1.8x 偏移，仍重叠** |

方法论沉淀：capstone 失步重同步、弱描述子场景不用 Lowe、票数不判别
要用加权分数。兴趣点几何重复性 52% = 管线健康底座。

## 5. 获胜方案（全部可溯源，🏆 2026-09-13 凌晨）

1. 11 取向脊线匹配滤波（抽头提取自 DLL）→ 增强图+取向图
2. 滤波响应极大值兴趣点 + 密度 NMS（52% 跨按压重复性）
3. 512bit 顶档描述子：4×4 区域 × 32 方向 bin，DLL 金字塔权重，
   主方向 = 获胜滤波取向
4. 海明 NN（预算 115/512，无 Lowe）→ 平移簇聚类（8px 桶众数，非迭代
   RANSAC）→ 角度众数投票
5. Windows 评分 Σ(128−h) − 未匹配特征惩罚(h/2)
6. 双帧模板库一致性（对抗位置特异偶然对齐）

## 6. 离线验证：完美分离

**判据 = 单帧最佳分数 ≥244 且 ≥2 帧模板库一致（分数≥150）**

| 会话 | probe 分数/一致帧 | 判定 |
|---|---|---|
| 真A（右指×右模） | 254/3帧, 403/4, 411/4 | ✓ |
| 真C（右指×右模） | 466/4, 280/3, 374/4 | ✓ |
| 假A（左指×右模） | 0, 199/3帧⚠, **438/1帧**, 0, 0, 239/1 | ✗（438 被 1 帧一致击杀）|
| 假C（左指×右模） | 213/1, 0, 147/0, 180/1, 209/2⚠, 189/1 | ✗ |

**FRR 0%（6/6），FAR 0%（0/12）**。⚠ = 单低分偶然一致，双条件天然过滤。

## 7. C 移植与真机集成验收

- **分数标度差异**：C 引擎的描述子位与 Python 参考略有不同（取向滤波
  插值路径不同）。**2026-09-13 重标定**（取向4 核修正 + 平局裁决修复 +
  权重越界钳制后，23 个 verify-run 共 125 个 probe，按运行取最高单帧分）：
  可信真手指运行最低 **349**，可信冒充运行最高 **288**（含存疑运行 321）
  → 阈值 **335**（区间中点），agree = **≥2 帧 × ≥150** 不变
  （修复前旧引擎的旧数字：真 336–470 / 假 127–280 / 阈值 300，已作废）
- **真机集成**（2026-09-13 凌晨，修复前引擎 + v0 模板）：右食指 ×3 =
  622/404/381 全 MATCH（早退判定 ~2s）；左食指冒充 = 285 正确拒绝。
  该组数字在修复引擎与 v1 模板（恢复取向字段）下失效，需重录重验
- **系统级**（fprintd 全链路，00:39，修复前引擎 + 旧阈值 300——现行
  335）：`probes=1 best_score=1086/300 => MATCH`，coverage 51%
- 与 Windows 原版差异（未移植）：阈值 660 自适应（−80/+60 封顶 1.5x）、
  验证期模板回馈（加密注册表 blob 'AE'，密钥 DAT_180038248）

## 8. 剩余保真度差距（历史清单，现为可选增强）

1. 描述子三档 29/49/69 字节 + 类型字节 + ID + atan2 LUT 全精度方向
   （现行固定 512bit 顶档已够用）
2. 二次机会 NN 惩罚（未匹配特征按 hamming/2 扣分）——已实现简化版
3. 逐位置脊线周期选核（现为全局 11 取向最大）
4. 0x15515 完整反汇编 = 位打包最终答案

## 9. 移植蓝图（来自反编译，版本无关的算法结构）

1.75x 重采样 → 平场归一化 → 脊线周期 FIR 组 → 结构张量 → 滤波响应
极大值兴趣点 → 亚像素+方向 → 11×11 二值方向直方图 → 海明 NN +
中值平移/角度一致几何投票 → score=Σ(128−hamming)（原版阈值 660，
自适应）。约束：每图 ≥11 特征、≥5 对一致、海明 ≤115/512、几何
8px/7°（C 版 12°）。参数现值见 `egis0575-matcher.h`。
