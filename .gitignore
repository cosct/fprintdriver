# 大体积本地内容（不随仓库发布）
# 驱动源码已在树内（libfprint/，subtree 保留上游历史），只忽略构建目录
libfprint/builddir*/
refs/
acerdrv/

# 指纹数据（生物特征，绝不发布）
datasets/
test-storage.variant

# makepkg 工作目录与产物
src/
pkg/
*.pkg.tar.zst
*.zip

# 打包工作目录与产物（本地跑 build-deb.sh / 复现 CI 打包会留下）
stage/
*.deb
*.rpm
*.tar.gz
/build/

# 本地跑 libfprint 示例（enroll）落在当前目录的指纹图像
enrolled.pgm

# 工具与缓存
.claude/
.mimosa/
__pycache__/
tools/egis0575-matcher-test
tools/eval_bz3

# ZCode/会话状态文件（本机工具产物）
thinking-effort-loaded.json
